# 约定与约束（实现细节）

本文件是项目规范约定的权威来源，AGENTS.md "二、项目规范"中的各节（代码生成、架构边界、质量守护、安全规范）为摘要引用，以本文件为准。AGENTS.md 中的通用规范（文件与文档、知识库管理等）不在本文件重复。

---

# 一、UI交互约定

## 页面布局

- {{UI 交互约定 1，如：手势、导航、布局规范}}

## 导航约定

- {{导航约定 1，如：Tab 切换、页面栈管理规范}}

---

# 二、编码约定

## 资源处理规则

- {{资源处理规则 1，如：图片/音频/视频的尺寸与格式约束}}
- {{资源处理规则 2，如：资源加载与缓存策略}}

## 特殊值处理规则

- {{特殊值处理 1，如：空值、占位符、保留字的定义与处理方式}}

## 配置与常量

- {{常量管理规则，如：统一收归到某个文件、分类命名规范}}
- {{运行时配置管理，如：配置文件加载方式}}

---

# 三、质量约定

## 编译规范

- 项目必须零警告，每次提交前通过构建验证。

## 错误处理约定

- 面向用户：自然语言提示，不含技术细节。
- 面向开发者：日志框架输出，可含错误码和上下文，禁止输出完整密钥（仅末四位）。
- {{其他错误处理规则}}

## 日志规范

- {{日志框架要求，如：禁止 print()，统一使用某日志框架}}
- {{日志分类与级别定义}}
- 日志文本禁用 emoji/加粗/斜体，禁止输出敏感字段。

## 线程与并发约定

- {{线程规则 1，如：UI 更新必须在主线程}}
- {{线程规则 2，如：异步操作的线程策略}}
- {{取消机制}}

## 内存与性能约定

- {{内存约定 1，如：大资源按需加载与缓存策略}}
- {{内存约定 2，如：列表页性能优化}}
- {{内存约定 3，如：数据存储分离}}

## 单元测试约定

- {{测试目录与命名规范}}
- {{测试覆盖要求}}
- {{Mock 注入方式}}

---

# 四、安全约定

## 密钥存储

- {{密钥存储方式，如：Keychain、Secret Manager 等}}
- {{配置文件中密钥的处理方式}}

## 网络安全

- 所有外部 API 用 HTTPS，不降级 HTTP。
- API 响应校验 HTTP 状态码和数据完整性。
- {{其他网络安全规则}}

## 数据隐私

- {{数据存储位置与隐私策略}}
- {{数据传输范围与最小化原则}}
- {{敏感配置文件的版本控制策略，如加入 .gitignore}}
