# 数据与类型边界

## 核心数据模型

{{描述核心数据模型，包含文件路径、协议、关键字段：}}
- {{模型名称}}（{{文件路径}}）：{{协议列表}}。字段：{{关键字段列表}}。

## 配置与设置

- {{配置结构描述，如：配置文件结构、设置管理方式}}

## 协调器输入输出

{{描述核心协调器的输入、进度、输出类型：}}
- {{协调器名称}}：输入 {{输入类型}}；进度 {{进度类型}}；完成 {{输出类型}}。

## 磁盘存储结构

```
{{存储根路径}}/
  {{目录/文件结构描述}}
```

{{加载与重组说明}}

## 配置文件结构

```json
{{配置文件 JSON 结构示例}}
```

{{配置加载优先级说明}}

## 持久化存储

{{描述非文件的持久化存储方式，如 UserDefaults、数据库等：}}
- {{存储键/表名}}：{{用途说明}}

## 边界约定

- {{边界约定 1，如：UI 层不直接构造/解析 API 请求与响应体}}
- {{边界约定 2，如：列表页只读轻量数据}}
- {{边界约定 3，如：全量加载的限制条件}}
