# 架构与模块边界

## 分层

- 表现层：{{表现层目录与主要视图/组件列表}}
- 业务协调：{{协调层目录与主要协调器}}
- 能力层：{{服务/管理层目录与主要类}}
- 系统集成：{{系统集成层目录，如：Intents、Widgets 等}}
- 数据与模型：{{数据模型目录与主要模型}}
- 预留：{{预留目录（如有）}}

## 模块边界

- {{边界规则 1，如：UI 不直接调底层 API，通过协调层调用}}
- {{边界规则 2，如：Coordinator 依赖关系}}
- {{边界规则 3，如：数据由哪个 Manager 读写}}
- {{边界规则 4，如：跨界面状态集中管理方式}}
- {{边界规则 5，如：跨模块协调方式}}

## 关键约束

- {{约束 1，如：全屏类型分类}}
- {{约束 2，如：导航结构}}
- {{约束 3，如：数据边界}}
