# Subagent: {{扫描维度名称}}扫描

## 任务

扫描项目源代码，检查是否符合{{扫描维度名称}}规范。逐文件读取并检查，输出违规清单。

## 扫描范围

{{扫描范围描述，列出需要扫描的目录和文件类型，使用绝对路径。示例：}}
- {{项目绝对路径}}/{{源码目录}}/ 目录下全部 {{文件扩展名}} 文件
- {{项目绝对路径}}/{{配置目录}}/ 目录下的配置文件（如需要）

## 检查规则

{{逐条列出检查规则，每条格式如下：}}
1. {{规则简称}} -- {{规则详细描述，包含具体的类名、方法名、约束条件}}
2. {{规则简称}} -- {{规则详细描述}}
3. {{规则简称}} -- {{规则详细描述}}

## 输出格式

```
## {{扫描维度名称}} 扫描结果

违规数量：N

| # | 文件 | 行号 | 违规项 | 建议修复 |
|---|------|------|--------|---------|
| 1 | ... | ... | ... | ... |

未发现违规的检查项：...
```

---

以下为常见扫描维度参考（每个维度对应一个独立的 subagent 文件）：

- 日志规范（scan-logging.md）：日志框架使用、敏感信息脱敏、错误信息分层
- 安全规范（scan-security.md）：密钥存储、HTTPS 强制、API 响应校验、数据本地化
- 架构边界（scan-architecture.md）：分层调用规则、导航管理、异步操作线程规则
- 编码约定（scan-conventions.md）：常量管理、特殊值处理、UI 交互约定
- 废弃代码（scan-dead-code.md）：未使用的类型/函数/变量/文件、无效导入
- {{自定义维度}}（scan-{{维度名}}.md）：{{维度描述}}
